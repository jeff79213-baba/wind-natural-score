{
  "name": "wind-natural-score",
  "version": "1.0.0",
  "description": "風自然訂房網分數查詢 - 截圖展示三家訂房網站分數",
  "private": true,
  "scripts": {
    "screenshot": "node scripts/screenshot.js --all",
    "build-data": "node scripts/build-data.js",
    "refresh": "node scripts/screenshot.js --all && node scripts/build-data.js",
    "serve": "npx http-server . -p 4100",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "playwright": "^1.49.0",
    "vitest": "^2.1.0"
  }
}
